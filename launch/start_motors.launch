<launch>

    <!--Bottom USB port next to HDMI port (Jetson Nano) -->
    <node name="serial_node" pkg="rosserial_python" type="serial_node.py">  
        <param name="port" type="string" value="/dev/ttyACM0"/>
    </node>

</launch>