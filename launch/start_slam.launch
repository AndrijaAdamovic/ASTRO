<launch>
    <arg name="simulation"  default="false"/>

    <include file="$(find astro)/launch/start_gmapping.launch"/>

    <group if="$(eval arg('simulation'))">
        <include file="$(find astro)/launch/simulation.launch">
        <arg name="view_in_rviz" value="false"/>
        </include>
    </group>

  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find astro)/rviz/slam_robot.rviz"/>
</launch>